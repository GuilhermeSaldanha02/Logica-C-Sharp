#oque estudar:

IF/ELSE
FOR/FOREACH
WHILE/DO-WHILE
ARRAY/LIST
SWITCH CASE

====================================================================
⦁	comando:
R= dotnet --version

⦁	comando:
R= dotnet --list--sdks

⦁	comando:
R= dotnet new console -o "nome" -n "nome"
-o (output): Define o diretório de saída. Se a pasta não existir, o .NET a cria automaticamente para você.
-n (name): Define o nome do projeto (o nome do arquivo .csproj).

⦁	comando:
R= dotnet new gitignore

⦁	comando: dotnet restore
R= Restaura todos os pacotes que a aplicação precisar para ser executada.

⦁	comando: dotnet clean
R= Limpa as compilações.(POR BOAS PRATICAS, SEMPRE ANTES DE DAR UM BUILD)

⦁	comando: dotnet build
R= Compila aplicação.

⦁	comando: dotnet run
R= Compila e executa a aplicação.
====================================================================
Logica e algoritmo

#Logica: O conceito é simples, pensar de maneira coerente/funcional a criação ou melhoria de um sistema.

#Algoritmo: É a estrutura de como o sistema ira funcionar.
====================================================================
#C# definições:
⦁	É uma linguagem de programação criada pelo Microsoft
⦁	C# é maduro, completo, popular e tem muito material.
⦁	É uma linguagem desafiadora mas que vai te dar uma ótima bagagem.
⦁	O C# é compilado incialmente para IL.
⦁	Depois o clr compila para o código de maquina, executa e gerencia.
⦁	IL é a linguagem intermediaria na qual linguagens da Microsoft são compiladas.
====================================================================
#FRAMEWORK:
⦁	.NET = É um framework de código aberto para desenvolvimento de sistemas desktop, web e mobile.
⦁	LTS = Long Term Support
⦁	Runtime = executa, SDK = cria
⦁	
====================================================================

====================================================================
ANOTÇÕES:
⦁	Using: será utilizado para importação**.**
⦁	Variáveis: uma variável é algo que utilizamos para armazenar uma informação, significa que o valor pode ser alterado.
⦁	Constantes: a definição é dada pela palavra reservada const antes do tipo.


TIPOS DE DADOS:

⦁	Primitivos: int, double,bool, stri ng.

====================================================================
====================================================================